# 个人使用 Python 常用函数集合

## 这是什么

用于保存日常用 Python 做科学计算和数据处理的代码, 减少重复造轮子. 主要功能有:

- 画图
- 数据处理
- 产生 SLM 用的全息图
- 产生 DMD 用的位图


## 用到的包

`numpy`, `matplotlib`, `math`, `scipy`, `pandas`, `PIL (pillow)`

此外, `imread` 函数也可以使用 `opencv` 来加快速度. 用 `timeit` 测试大约快四倍. 注意这只是测试并不是实际表现.

但是, `opencv` 是一个比较大型且专业的库, 我觉得如果能避开使用这样大型的库会更加方便. 我暂时不清楚不用 `opencv` 慢多少, 如果不多的话我会考虑放弃使用 `opencv`. 


## 优点

- 比直接调用 Python 更简单, 因为只保留了我认为常用的功能
- 画的图默认矢量图显示 (如果可以的话) 
- 字体默认使用思源黑体. 因为有独立的字体文件所以不需要安装字体
- 改进后的 plot 函数, 可以直接输入定义域和函数名画图, 推荐使用 `lambda` 语句十分方便


## 可能还可以优化的点

- 应该可以再一步充分利用 `numpy` 的并行优势, 把输入给估计子函数的参数改成张量, `numpy` 就可能可以并行计算所有图片. 但是由于目前速度的提高带来的收益可能并不大了, 故暂时不考虑但先记下备忘
- 把工作区作为一个类, 让 Python 创建文件夹, 读取文件夹, 后续操作都变成对工作区操作. 这样应该可以更方便, 有时间甚至可能可以做一个 GUI. 目前暂时用不到, 也先记下备忘
- 如何实现自由地计算叠加模式? 目前做标准HG模式已经可以实现了, 但是如果想要生成叠加模式还不行. 只能用复振幅或者振幅手动加起来的方式实现. 但是按理说, 例如 +- 模式, 他其实也是一个正交且近完备的模式, 按理说应该也要能作为激光的一个参数用于计算. 但是我还不知道怎么实现这个功能. 


## 如何使用

推荐把整个文件夹放到 Anaconda 的 `site-packages` 文件夹下, 可以直接 `import mypy`

- 想知道 `site-packages` 在哪?

```python
import numpy as np
print(np.__file__)
```